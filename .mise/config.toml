[tools]
node = "20"
pnpm = "9"

[tasks.validate]
description = "Run all validation checks (install, typecheck, lint, test, knip)"
run = """
pnpm install --frozen-lockfile
pnpm typecheck
pnpm lint
pnpm test
pnpm knip
"""
