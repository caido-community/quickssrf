[tools]
node = "22"
pnpm = "10"

[tasks.validate]
description = "Run all validation checks (install, typecheck, lint, test, knip)"
run = """
pnpm install --frozen-lockfile
pnpm typecheck
pnpm lint
pnpm test
pnpm knip
"""
