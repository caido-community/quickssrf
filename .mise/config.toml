[tools]
node = "24.11.1"
pnpm = "10.28.2"

[tasks.validate]
description = "Run all validation checks (install, typecheck, lint, test, knip)"
run = """
pnpm install --frozen-lockfile
pnpm typecheck
pnpm lint
pnpm test
pnpm knip
"""
